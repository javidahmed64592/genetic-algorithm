[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
numpy = "~=1.25"

[dev-packages]
pytest = "*"
pytest-cov = "*"
ruff = "*"
ipykernel = "*"

[requires]
python_version = "3.12"

[scripts]
test = "python -m pytest -vx --cov=genetic_algorithm --cov-report term-missing"
ruff = "python -m ruff check ."
